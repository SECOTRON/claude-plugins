## What

Briefly describe the change.

## Why

What problem does it solve, or what does it add?

## Plugin(s) affected

e.g. `session-finder`, marketplace-wide tooling, docs only.

## Checklist

- [ ] Conventional Commit messages (`type(scope): ...`)
- [ ] `make test` passes locally (manifests valid, lint clean, smoke OK)
- [ ] Docs / README updated if behavior or usage changed
- [ ] For a new plugin: added under `plugins/<name>/` and registered in `marketplace.json`
